import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.cerbaro.musat',
  appName: 'Musat Assistencia Técnica',
  webDir: 'dist'
};

export default config;
